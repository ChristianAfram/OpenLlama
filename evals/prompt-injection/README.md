# prompt-injection

Proves a malicious instruction embedded in repository content cannot cause a
mutation or secret leak — even if the model is fully compromised and obeys it.

Cases: [`src/evals/cases/prompt-injection.ts`](../../src/evals/cases/prompt-injection.ts).
Gate: **100%** (hard release gate, Master Plan §11).
